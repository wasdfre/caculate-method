function [n1,n2] = Fa( j,n )

% 结果为函数次数与求导次数差或0
n2=max(j-n,0);
% 初始系数为1
n1=1;
for k=0:n-1
    % 每求一次导，前面系数*
   n1=n1*(j-1-k); 
end
%如果求导次数为0
if(n==0)
    %系数为1
    n1=1;%前方系数
    n2=j-n;%剩余次数
end


end
